<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.e.museamapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MuseamApp">
        <activity android:name=".Noguchi" android:label="@string/noguchi"></activity>
        <activity android:name=".NewMuseum" android:label="@string/newmuseum"/>
        <activity android:name=".MuseumofJewish" android:label="@string/jewish"/>
        <activity android:name=".MuseumofChinese" android:label="@string/chinese" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>